require 'spec_helper'

module NavalConflict
  describe TaskForce do
    # let( :options )          { options = { :name => 'Scapa Flow', :max_allowed_combat_aircraft => 100 }  }
    # let( :strike_aircraft )  { ["F15", 10] }
    # let( :target )          { double( 'target', :id => 11, :name => "Task Force #{:id}")}
    # 
    # before( :each ) do
    #   @airbase = Airbase.new( options )
    #   @airbase.add_aircraft( "F14", 20 )
    #   @airbase.add_aircraft( "F15", 20 )
    # end

    it "should have a name" do
      pending
    end
    
    it "should allow adding ships" do
      pending
    end
    
    it "should allow removing ships" do
      pending
    end 
    
    it "should allow one or more ships to be scuttled" do
      pending
    end
    
    it "max speed is the max speed of the slowest ship" do
      pending
    end
    
    it "should have a missile defense value" do
      pending
    end
    
    it "should have an anti-aircraft value" do
      pending
    end
    
    it "should have a mission type" do
      pending
    end
    
    it "should have an electronic warfare mode" do
      pending
    end
    
    it "should be able to dock with a harbor" do
      pending
    end
    
    it "should be able to off-load supplies at a harbor" do
      pending
    end
    
    it "should place its ships in the repair and refit queue when it docks with a homeport" do
      pending
    end
    
    it "should be able to bombard an enemy harbor if its mission type is bombardment" do
      pending
    end
    
    it "should be able to initiate surface combat if its mission type is combat" do
      pending
    end
    
    it "should be able to include transport ships if its mission type is transport" do
      pending
    end
  end  
end